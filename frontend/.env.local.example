# API gateway (existing platform)
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# Orchestrator UI (off by default — enable when unishield orchestrator is running on :8001)
NEXT_PUBLIC_ORCHESTRATOR_UI=true
NEXT_PUBLIC_ORCHESTRATOR_DASHBOARD_METRICS=true
